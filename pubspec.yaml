name: azerox
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.5.1
  dio: ^4.0.4
  intl: ^0.17.0
  flutter_social_content_share: ^1.0.2
  flutter_facebook_auth: ^3.5.7
  audioplayers: ^0.20.1
  cached_network_image: ^3.2.0
  image_picker: ^0.8.4+8
  permission_handler: ^8.3.0
  audio_session: ^0.1.6+1
  flutter_sound: ^9.1.7
  flutter_sound_platform_interface: ^9.1.7
  youtube_player_iframe: ^2.2.2
  dropdown_button2: ^1.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
